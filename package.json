{
	"name": "@hbxcontrols/conversion",
	"version": "2.0.3",
	"description": "A set of tools for converting units of measurement for HBX applications",
	"main": "src",
	"scripts": {
		"release": "standard-version && git push --follow-tags",
		"release-publish": "standard-version && git push --follow-tags && npm publish"
	},
	"author": "Chris Tohill <graphics@hbxcontrols.com>",
	"license": "MIT",
	"dependencies": {
		"number-precision": "^1.6.0",
		"standard-version": "^9.5.0"
	}
}
